<%- include('./partials/header.ejs') %>

    <% if (locals.bandName) { %>
        <h1><%= bandName %></h1>
    <% } else { %>
        <h1>Welcome to the Band Generator 🤟</h1>
    <% } %>
    <div class="container">
        <form class="backBtn" action="/" method="POST">
            <button onclick="document.querySelector(this).parentNode.submit()">
                <i class="fa-solid fa-arrow-left" style="color: white; font-size: 20px;"></i>
            </button>
        </form>
        <form action="/submit" method="POST">
            <input type="submit" value="Generate Name">
        </form>
    </div>

<%- include('./partials/footer.ejs') %>